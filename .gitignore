INSTALL
aclocal.m4
ar-lib
autom4te.cache
compile
config
configure
depcomp
install-sh
libtool
ltmain.sh
m4
missing
py-compile
stamp-h1
test-driver

.deps
.libs

*.la
*.lo
*.o
*.pyc

# protobuf-generated sources (regenerated by protoc during the build)
*.pb.cc
*.pb.h
*_pb2.py

# generated roconfig blob + its text dump + the concatenated generator
# (SOURCE lives in proto/roconfig/*.pb.text and proto/roconfig_gen_{head,tail}.cpp)
/proto2/roconfig.pb
/proto2/roconfig.pb.text
/proto/roconfig_gen.cpp
/proto/roconfig_gen

# autotools-generated (config.h.in is the tracked template; the rest are generated)
config.h
config.status
config.log
*.log
*~

Makefile
Makefile.in

# secrets — never commit RPC keys / private keys (the live .env lives outside this repo)
.env
.env.*
*.key
*.pem
